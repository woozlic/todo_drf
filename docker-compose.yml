version: '3.1'

services:
  db:
    image: postgres:13.4
    privileged: true
    ports:
      - 24322:5432
    environment:
      POSTGRES_USER: woozlic
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: todos

  backend:
    build: ./backend
    privileged: true
    ports:
      - 8080:8080
    command: bash -c "
        python manage.py migrate &&
        python manage.py create_users &&
        gunicorn todo_drf.wsgi -b 0.0.0.0:8080
      "
    depends_on:
      - db

  nginx:
    build: ./nginx
    ports:
      - 8000:80
    depends_on:
      - backend

  frontend:
    build: ./frontend
    ports:
      - 80:80
    depends_on:
      - nginx